# テーマ制作ガイド（毎日おはV）

新しい日のテーマを足すときは、**このファイルだけ読めば作業できる**ようにしてある。
基幹（`core/`・`index.html`）は触らない。足すのは `assets/themes/YYYY-MM-DD/` の1フォルダだけ。

---

## 0. 最初に見るもの（この順で）

| 順 | ファイル | 何のため |
|---|---|---|
| 1 | `THEME_GUIDE.md`（このファイル） | ルール・手順・確認方法のすべて |
| 2 | `assets/themes/_template/theme.js` | コピー元。最小構成（アクスタ＋カレンダー＋カップ） |
| 3 | 近い雰囲気の既存テーマ `assets/themes/2026-09-26〜2026-10-01/theme.js` | 作り込みの基準（この水準が最低ライン） |
| 4 | `core/engine.js` の該当部分 | 部品の細かい使い方が必要なときだけ（`GEN` `MAT` `props` `acrylicStand` `dateProp`） |

作業フォルダは利用者のPCの「毎日おはV」フォルダ（デスクトップ）。完成したファイルはそこへ書き戻す。

---

## 1. 作業手順

1. **テーマを決める**：その日の「◯◯の日」から、机の上などに置いて絵になるものを選ぶ
2. `assets/themes/_template` をコピーし、フォルダ名をその日の日付（例 `2026-10-02`）にする
3. `theme.js` の `id`（英字）・`title`（日本語の「◯◯の日」）・`dayName`（英語）・`caption` の色を書く
4. `render()` にシーンを作る（下の「2. 必須ルール」を守る）
5. **描画確認**：`python3 tools/check/render.py 2026-10-02 --zoom`
   - `tools/check/out/2026-10-02.png`（全体）と `_zoom1〜4.png`（4分割の拡大）ができる
   - `OK` 以外（`NG`・エラー行）が出たら直す。全体と拡大図の両方を目で見る
6. 「4. 仕上げチェックリスト」をすべて満たすまで 4〜5 を繰り返す
7. 利用者のフォルダへ書き戻し、書き戻した先の中身が一致しているか確かめる（チェックサムなど）

一覧ファイルの編集は不要。フォルダを置けば、その日の7日前から日付タブに出てくる。

---

## 2. 必須ルール

### 構図（全テーマ共通）
- 立ち絵は**アクリルスタンド**で置く：`E.acrylicStand(env)` → `stand.draw(api, { x, z, yaw })`。寸法は共通（変えない）
- 飽きないよう `yaw`（向き）とカメラ位置で変化をつける。カメラは既存テーマとほぼ同じ高さ・画角（fov 31前後）
- 画面の上下はキャプションが重なる。小物は「アクスタの左右」と「その奥」に置く
- 立ち絵の色味は変えない（アクスタが自動で処理する。上からフィルターをかけない）

### 文字
- 上の「Good Morning」と下の `dayName` は基幹が自動で重ねる。テーマは `dayName` と色だけ書く
- **画角内に「MM/DD」を必ず入れる**：`E.dateProp(env, { style })` の卓上カレンダー（壁なら `form: 'wall'`）
  - 隠れない・画面端で切れない・他の物にめり込まない位置に置く。ぼけすぎない奥行きに
- 絵の中に日本語を描くなら、その文字を `fontText` に書く（書かないとフォントが揃わずエラーになる）

### 作り込みの水準（9/26〜10/01 が最低ライン）
- **物の数より、1つずつの完成度**。数を増やして密度を出さない
- 形は `api.lathe`（回転体）・`api.rbox`（角丸の箱）・`api.mesh` ＋ `E.GEN.surface / tube`（曲面・管）で作る。
  箱と円柱を寄せ集めただけの物は置かない
- 質感は `E.MAT`（ceramic / matte / wood / metal / gold / plastic / glossyFood / cream / glass）を元に色を変えて使う
- 食べ物・器は特に丁寧に：断面・クリーム・果物・縁の厚み・取っ手や注ぎ口のつながりまで形で作る
- 地面・テーブル・壁は**ぱっと見て何か分かる**ものにする（木目、ギンガムチェックのクロス、タイルなど）。
  ひび割れや汚れに見える模様（細い不規則な線の大理石など）は使わない
- 細かい縞のテクスチャは縮小でちらつく（モアレ）。線は粗く・淡く

### 置かないもの（小物の配置ルール）
物理的な重なり・支えが不自然になりやすいので、次は**置かない**。
- スプーン・フォーク・ナイフ・箸・マドラーなどの小さい什器
- ペン・鉛筆・羽根ペン・眼鏡・鍵・クリップ・ケーブル類などの細い小物
- 散らばった粒（コーヒー豆・菓子・ビーズなど）
- 物の上に小物を載せる・差す・立てかける（ペン立ての鉛筆、インク壺の羽根ペン、本の上の眼鏡など）
- 紙やカードを何枚も重ねる

置いてよいのは、**床・机に直接置く**はっきりした形の物と、**支えが明らかな組み合わせ**
（カップ＆ソーサー、ケーキスタンドの上のケーキ、平積みの本、瓶と蓋 など）。

### 描画スタイル（全テーマ共通：線＋セル塗り）
- 標準は **`toon`**（物の縁に線、陰影は光・中間・影の3段、影は少し青紫、ツヤは輪郭のはっきりした形）。エンジンが自動でかけるので、テーマ側の指定は不要
- 写実寄りの質感（細かい凹凸・粒子ノイズ）で作り込まない。形と色の塗り分けで見せる。`bump` は効かない
- アクスタ（立ち絵）と日付カレンダーの文字は対象外（エンジン側で除外済み）
- 線の太さ・影色などの値は `core/engine.js` の `STYLE` にある。変えるのは基幹の変更なので、テーマ制作では触らない
- 比較用に `python3 tools/check/render.py 日付 --style=real|line|toon|illust|soft` で他のスタイルも描ける（公開ページは常に標準）

### ライティング（通常の室内照明の範囲）
- ドラマティックにしない。強いスポット・濃い影・白飛び・暗い周辺落ちで演出しない
- 目安：`ambient` 0.45〜0.6 ／ `lightCol` 各成分 1.0〜1.2 ／ `E.drawVignette` 0.2 以下
- `sky` `ground`（環境光の上下の色）と `envTop` `envBot`（金属の映り込み）はシーンの色に合わせる
- `ctx` に後から重ねる色（`globalCompositeOperation` での光の追加など）は弱く（0.2以下）

### 失敗時の扱い（黙って代わりのもので続けない）
- `if (!GLX.available()) { 塗りつぶして return }` のような逃げ道をテーマに書かない
- 画像・フォントが読めなければ基幹がエラーを表示して保存を止める。テーマ側で握りつぶさない（`try/catch` で隠さない）

---

## 3. 部品の早見表

```js
const S = E.Stage3D, G = E.GEN, M = E.MAT, K = E.props();
S.render(ctx, { W, H, clear, ambient, light, lightCol, sky, ground, envTop, envBot, camera: { eye, at, fov, focus, dofScale, blur }, draw(api) { … } });
```

| 置き方 | 用途 |
|---|---|
| `api.box / quad / panel / cylinder / sphere` | 床・壁・天板など単純な面（小物には使わない） |
| `api.lathe(key, [[半径, 高さ, 角], …], pos, rot, size, mat)` | 器・瓶・台座・果物（回転体） |
| `api.rbox(pos, rot, size, { round })` | 本体・キー・表紙など（角の丸い箱。細長い物は round 小さめ） |
| `api.mesh(key, () => G.surface(f, nu, nv) \| G.tube(path, r, …), pos, rot, size, mat)` | 自由曲面・取っ手・管 |
| `K.cup / saucer / plate / pitcher / book / plant` | 陶器・本・観葉植物（キット） |
| `K.shadow(api, x, z, 幅, 奥行, yaw, 濃さ)` | 足元の柔らかい接地影（光の影は自動） |
| `K.texture(canvas, { repeat })` | 自作テクスチャ（`K.free()` で一緒に解放） |
| `api.blend(true) … api.blend(false)` | 半透明。奥から順に描き、アクスタは最後 |

- `rot` は度で `[X, Y, Z]`。`Y` は上から見た向き、`Z` を正にすると上端が −X（左）へ倒れる
- `draw(api)` は影・色・被写界深度の3回呼ばれる。乱数で位置を決めるなら `E.rnd(種)` で毎回同じにする
- 描画後に `stand.free(); cal.free(); K.free();`

---

## 4. 仕上げチェックリスト

- [ ] `tools/check/render.py … --zoom` が `OK`、警告・エラー行なし
- [ ] カレンダーの「MM/DD」と曜日・テーマ名が読める（隠れ・切れ・めり込みなし）
- [ ] 拡大図で、めり込み・浮き・隙間・形の崩れ・モアレがない
- [ ] 「置かないもの」を置いていない
- [ ] 地面・背景が何か一目で分かる
- [ ] ライティングが目安の範囲（白飛び・真っ黒の影がない）
- [ ] 線が物の輪郭に沿って出ている（途切れ・二重線・背景の不自然な線がない）
- [ ] 立ち絵がキャプションに大きく隠れていない（初期位置で顔と上半身が見える）
- [ ] `node --check` が通る／フォールバック分岐を書いていない
- [ ] 利用者のフォルダへ書き戻し、中身の一致を確認した
